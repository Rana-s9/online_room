<header>
    <nav class="bg-dark-beige border-2 border-matcha/75 text-matcha text-sm md:text-lg font-semibold fixed top-0 w-full z-50">
      <div class="max-w-screen-xl flex items-center justify-between mx-auto p-2 md:p-4">
        <div class="text-md md:text-2xl">
          <%= link_to "Online Room", root_path, data: { turbo: false } %>
        </div>
        <div>
          JA/EN(実装前)
        </div>
        <div class="flex">
          <%= link_to "Tips", posts_path, data: { turbo: false } %>
        </div>
        <div class="flex space-x-4">
          <%= link_to "ログイン(帰宅)", new_user_session_path, data: { turbo: false } %>
          <%= link_to "新規登録", new_user_registration_path, data: { turbo: false } %>
        </div>
      </div>
    </nav>
  </header>