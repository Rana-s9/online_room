<header>
    <nav class="border-2 border-matcha/75 text-matcha font-semibold fixed top-0 w-full z-50">
      <div class="max-w-screen-xl flex items-center justify-between mx-auto p-4">
        <div>
          日本語/English
        </div>
        <div class="text-xl text-center">
          <%= link_to "Online Room", root_path, data: { turbo: false } %>
        </div>
        <div class="flex space-x-2">
          <%= link_to "玄関に行く", rooms_path,data: { turbo: false } %>
        </div>
        <div class="flex space-x-4">
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete ,data: { turbo: false } %>
        </div>
      </div>
    </nav>
  </header>

